Instructions

$ bundle install

$ yarn install

$ yarn build --watch

$ rails db:create db:migrate db:seed

$ rails s

Troubleshooting

if bundle install doesn't work, do

$ bundler install

$ bundle install
