<turbo-frame id="save_button_<%= @post.id %>">
  <%= button_to saved_for_later_path(@saved_for_later), class: "icon-button", method: :delete do %>
    <i class="fas fa-bookmark"></i>
  <% end %>
</turbo-frame>
